This directory contains Spatie Data DTOs used by the Form Builder package.

To install the Spatie DTO dependency and run the package test-suite execute:

composer require spatie/laravel-data
./vendor/bin/phpunit --configuration packages/form-builder/phpunit.xml
