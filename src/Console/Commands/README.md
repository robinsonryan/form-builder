This directory contains artisan commands provided by the Form Builder package.

- forms:seed-sample
  Seeds a minimal sample global form and a published version for local development and testing.

- forms:publish
  Placeholder for the publish pipeline (compose slots/fragments, derive steps, validate, persist).

- forms:lint
  Placeholder for a lint command that would validate JSON schemas.

Run via:
  php artisan forms:seed-sample
